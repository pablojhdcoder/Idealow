/** Entrada normalizada para los validadores (alineada con extractor + síntesis del wizard). */
export type ValidationIdeaInput = {
  elevator_pitch: string
  problem_statement: string
  search_keywords: string[]
}
